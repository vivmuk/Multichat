{
  "name": "venice-multi-chat-app",
  "version": "1.0.0",
  "description": "Venice AI Multi-Chat Application",
  "scripts": {
    "start": "npx serve . --single --listen 3000",
    "build": "node build.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

